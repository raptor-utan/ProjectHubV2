# AGENTS.MD

## 目的

このファイルは、このリポジトリで作業する AI エージェント向けの標準ルールを定義する。
より深い階層に別の AGENTS.MD が存在しない限り、本ドキュメントのルールをリポジトリ全体に適用する。

---

## 技術スタック
バックエンド：ActixWeb、Rust
フロントエンド：TypeScript、React、HTML、CSS、BootStrap
データベースアクセス：任意のライブラリ


## 基本方針

1. 人間が読みやすく、保守しやすいコードを優先する。
2. 仕様・設計・実装履歴を分離して記録する。
3. 過剰な抽象化や過剰な分割は避ける。
4. AIの推測で仕様を補完せず、不明点は明示する。
5. 既存の設計・命名・ディレクトリ構成を優先する。

---

## 開発手順

1. ユーザーからの指示や仕様を受け取った場合、必要に応じて spec.md に記録する。
2. spec.md を基に、作業内容を todo.md に分解する。
3. 構造変更・設計判断が必要な場合は architecture.md に記録する。
4. 実装・変更・修正を行った場合は work-journal.md に概要を1行で記録する。
5. 大きな機能追加やユーザーから要求がある場合は、画面キャプチャ等を含むHTML形式の解説書を作成する。
6. 軽微な修正の場合は、todo.md と work-journal.md のみ更新してよい。

---

## ドキュメントの役割

### spec.md

ユーザー要求、仕様、前提条件、制約、未決事項を記録する。

### todo.md

実装すべき作業をチェックリスト形式で記録する。

### architecture.md

アプリケーション構造、責務分割、主要な設計判断を記録する。

### work-journal.md

実施した作業の概要を時系列で記録する。
各項目は1行程度とし、詳細な実装内容までは記録しない。

---

## コーディング規約

1. データベースを扱う場合は、処理内容に応じて ORM・生SQL・またはその併用を適切に選択する。
2. ID、コード、管理番号など、同じ基底型でも意味が異なり混同しやすい値には、必要に応じて NewType を使用する。
3. 循環的複雑度は原則10以下を目安とし、複雑な処理は適切に分割する。
4. 変数名・関数名・クラス名は、処理内容や責務が推測できる名前にする。
5. 公開クラス、公開メソッド、サービス層、Repository層、複雑な内部関数には日本語Docstringを記載する。
6. データバリデーションが必要な場合は Pydantic を使用する。
7. 定数は constants.py または constants/ 配下の専用ファイルに定義する。
8. マジックナンバー、マジック文字列は避け、意味のある定数名を付ける。
9. 既存コードの命名規則、ディレクトリ構成、設計方針を優先する。
10. 変更範囲は必要最小限に留める。
11. 不要なリファクタリングや、仕様外の機能追加は行わない。

---

## テスト方針

1. 既存テストがある場合は、変更後に関連テストを実行する。
2. テストが存在しない場合は、重要なロジックに対して必要に応じてテストを追加する。
3. 外部API、DB、ファイルシステムを扱う処理は、可能な範囲でモック化または分離する。
4. テスト実行ができない場合は、その理由を work-journal.md に記録する。

---

## 禁止事項

1. 仕様にない大規模な構造変更を勝手に行わない。
2. 既存の公開APIやDBスキーマを無断で変更しない。
3. マジックナンバーや意図不明な短縮名を使用しない。
4. 複雑な処理を1つの関数・メソッドに詰め込まない。
5. 推測だけで仕様を確定しない。